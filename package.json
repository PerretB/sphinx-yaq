{
  "name": "demo-sphinx-yaq-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npm run test:js",
    "test:js": "npm run check:js && vitest run --coverage",
    "test:js:watch": "vitest",
    "test:e2e": "playwright test",
    "check:syntax": "node --check frontend/src/grading.js && node --check frontend/src/model.js && node --check frontend/src/runtime.js && node --check frontend/src/storage.js",
    "build:js": "node scripts/build-js.mjs",
    "check:js": "node scripts/build-js.mjs --check"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.63.0",
    "@vitest/coverage-v8": "^4.1.11",
    "esbuild": "^0.28.2",
    "jsdom": "^27.0.0",
    "vitest": "^4.0.0"
  }
}
