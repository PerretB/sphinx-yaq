{
  "name": "demo-sphinx-yaq-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npm run test:js",
    "test:js": "npm run check:js && vitest run --coverage",
    "test:js:watch": "vitest",
    "build:js": "node scripts/build-js.mjs",
    "check:js": "node scripts/build-js.mjs --check"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.11",
    "esbuild": "^0.28.2",
    "jsdom": "^27.0.0",
    "vitest": "^4.0.0"
  }
}
